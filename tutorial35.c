#include <stdio.h>
#include <string.h>
int main()
{
      char s1[] = "Arun";
      char s2[] = "Harry";
      char s3[63];
      printf("The stream for s1,s2 returned %d",strcmp(s1,s2));
      // puts(strcat(s1,s1));
      // printf("The length of s1 is %d\n",strlen(s1));
      // printf("The length of s2 is %d\n",strlen(s2));
     // printf("The reversed string s1 is:");
     // puts(strrev(s1));
     // printf("The reversed string of s2 is:");
     // puts(strrev(s2));
     // printf("the copy of string is:");
      // puts(strcpy(s1,s2));
     // strcpy(s3, strcat(s1, s2));
     // puts(s3);

      return 0;
}